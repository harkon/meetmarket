{{> header}}
<div class="container">
  <div class="row">
    <div class="col-md-6">
      <h2>List</h2>
      <div class="list-group">
        {{#each stores}}
        <a href="/stores/{{_id}}" class="list-group-item">{{name}}</a>
        {{/each}}
      </div>
    </div>
    <div class="col-md-6">
      <h2>Preview</h2>
    </div>
  </div>
</div>