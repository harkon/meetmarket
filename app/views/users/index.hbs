<div id="wrapper">
  {{> navigation}}
  <div class="container">
    <div class="row">
      <h2>OSS list</h2>
      <div class="col-md-8">
        <ul class="nav nav-pills nav-justified">
          <li><a href="/users/{{user_id}}">Home</a></li>
          {{debug this}}
          {{#each _stores}}
          <li><a href="/stores/{{_id}}">{{title}}</a></li>
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>